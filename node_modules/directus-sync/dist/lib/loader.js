"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.initContext = initContext;
exports.disposeContext = disposeContext;
exports.loadCollections = loadCollections;
const services_1 = require("./services");
const helpers_1 = require("./helpers");
const typedi_1 = require("typedi");
// eslint-disable-next-line @typescript-eslint/require-await
function initContext(programOptions, commandOptions) {
    return __awaiter(this, void 0, void 0, function* () {
        // Get the config service
        const config = typedi_1.Container.get(services_1.ConfigService);
        // Set the options
        config.setOptions(programOptions, commandOptions);
        // Set the logger level from the config
        typedi_1.Container.get(services_1.LoggerService).setLevel(config.getLoggerConfig().level);
        (0, helpers_1.createDumpFolders)();
    });
}
function disposeContext() {
    // Close some services if needed
}
function loadCollections() {
    // The order of the collections is important
    // The collections are populated in the same order
    const collectionsConstructors = {
        settings: services_1.SettingsCollection,
        folders: services_1.FoldersCollection,
        translations: services_1.TranslationsCollection,
        flows: services_1.FlowsCollection,
        operations: services_1.OperationsCollection,
        roles: services_1.RolesCollection,
        policies: services_1.PoliciesCollection,
        permissions: services_1.PermissionsCollection,
        dashboards: services_1.DashboardsCollection,
        panels: services_1.PanelsCollection,
        presets: services_1.PresetsCollection,
    };
    // Get the collections to process
    const config = typedi_1.Container.get(services_1.ConfigService);
    const logger = typedi_1.Container.get(services_1.LoggerService);
    const collectionsToProcess = config.getCollectionsToProcess();
    const excludedCollections = services_1.CollectionsList.filter((collection) => !collectionsToProcess.includes(collection));
    const sortedCollections = Object.keys(collectionsConstructors);
    const sortedCollectionsToProcess = sortedCollections.filter((collection) => collectionsToProcess.includes(collection));
    // Initialize the collections
    const output = [];
    for (const collection of sortedCollectionsToProcess) {
        const collectionConstructor = collectionsConstructors[collection];
        output.push(typedi_1.Container.get(collectionConstructor));
    }
    if (excludedCollections.length > 0) {
        logger.debug(`Excluded collections: ${excludedCollections.join(', ')}`);
    }
    return output;
}
//# sourceMappingURL=loader.js.map