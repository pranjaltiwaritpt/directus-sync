"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ConfigFileLoader = void 0;
const schema_1 = require("./schema");
const fs_extra_1 = require("fs-extra");
const deepmerge_1 = __importDefault(require("deepmerge"));
const path_1 = __importDefault(require("path"));
const helpers_1 = require("../../helpers");
class ConfigFileLoader {
    constructor(configPath) {
        this.configPath = configPath;
        this.loadedConfigPaths = [];
        this.loadedConfig = this.loadFromPath(this.configPath);
    }
    get() {
        return this.loadedConfig;
    }
    loadFromPath(configPath) {
        // Check if the config is already loaded
        // If the config is already loaded, return undefined to avoid infinite loop
        if (this.loadedConfigPaths.includes(configPath)) {
            return undefined;
        }
        // Check if the config exists
        if (!(0, fs_extra_1.existsSync)(configPath)) {
            return undefined;
        }
        // eslint-disable-next-line @typescript-eslint/no-require-imports
        const rawConfig = require(configPath);
        // Validate and return the config
        const config = (0, helpers_1.zodParse)(rawConfig, schema_1.ConfigFileOptionsSchema, 'Config file');
        // Add the config to the loaded config
        this.loadedConfigPaths.push(configPath);
        // Merge with parents
        return this.mergeWithParents(config, configPath);
    }
    mergeWithParents(config, currentPath) {
        const extendsPath = config.extends;
        if (!extendsPath) {
            return config;
        }
        let mergedConfig = config;
        for (const parentPath of extendsPath) {
            const parentFullPath = path_1.default.resolve(path_1.default.dirname(currentPath), parentPath);
            const parentConfig = this.loadFromPath(parentFullPath);
            if (!parentConfig) {
                continue;
            }
            mergedConfig = (0, deepmerge_1.default)(parentConfig, mergedConfig);
        }
        return mergedConfig;
    }
}
exports.ConfigFileLoader = ConfigFileLoader;
//# sourceMappingURL=config-file-loader.js.map