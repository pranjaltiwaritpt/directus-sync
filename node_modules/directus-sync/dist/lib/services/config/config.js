"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ConfigService = void 0;
const typedi_1 = require("typedi");
const path_1 = __importDefault(require("path"));
const typescript_cacheable_1 = require("typescript-cacheable");
const config_file_loader_1 = require("./config-file-loader");
const helpers_1 = require("../../helpers");
const deepmerge_1 = __importDefault(require("deepmerge"));
const default_config_1 = require("./default-config");
const schema_1 = require("./schema");
const logger_1 = require("../logger");
let ConfigService = class ConfigService {
    setOptions(programOptions, commandOptions) {
        this.programOptions = programOptions;
        this.commandOptions = commandOptions;
    }
    getLoggerConfig() {
        return {
            level: this.requireOptions('debug') ? 'debug' : 'info',
        };
    }
    shouldSortJson() {
        return this.requireOptions('sortJson');
    }
    getCollectionsConfig() {
        const dumpPath = path_1.default.resolve(this.requireOptions('dumpPath'));
        const collectionsSubPath = this.requireOptions('collectionsPath');
        const collectionsPath = path_1.default.resolve(dumpPath, collectionsSubPath);
        return {
            dumpPath: collectionsPath,
        };
    }
    getSnapshotConfig() {
        const dumpPath = path_1.default.resolve(this.requireOptions('dumpPath'));
        const snapshotSubPath = this.requireOptions('snapshotPath');
        const snapshotPath = path_1.default.resolve(dumpPath, snapshotSubPath);
        return {
            dumpPath: snapshotPath,
            splitFiles: this.requireOptions('split'),
            force: this.requireOptions('force'),
            enabled: this.requireOptions('snapshot'),
            prettyDiff: this.requireOptions('prettyDiff'),
        };
    }
    getSpecificationsConfig() {
        const dumpPath = path_1.default.resolve(this.requireOptions('dumpPath'));
        const specificationsSubPath = this.requireOptions('specsPath');
        const specificationsPath = path_1.default.resolve(dumpPath, specificationsSubPath);
        return {
            dumpPath: specificationsPath,
            enabled: this.requireOptions('specs'),
        };
    }
    getSeedConfig() {
        const paths = this.requireOptions('seedPath');
        const seedPaths = Array.isArray(paths) ? paths : [paths];
        const seedFullPaths = seedPaths.map((p) => path_1.default.resolve(p));
        return {
            paths: seedFullPaths,
        };
    }
    getPushConfig() {
        return {
            maxPushRetries: this.requireOptions('maxPushRetries'),
        };
    }
    /**
     * Returns the Directus config, either with a token or with an email/password
     */
    getDirectusConfig() {
        const url = this.requireOptions('directusUrl');
        const directusConfig = this.getOptions('directusConfig');
        const clientConfig = directusConfig === null || directusConfig === void 0 ? void 0 : directusConfig.clientOptions;
        const restConfig = directusConfig === null || directusConfig === void 0 ? void 0 : directusConfig.restConfig;
        const token = this.getOptions('directusToken');
        if (token) {
            return { url, token, clientConfig, restConfig };
        }
        const email = this.requireOptions('directusEmail');
        const password = this.requireOptions('directusPassword');
        return { url, email, password, clientConfig, restConfig };
    }
    getUntrackConfig() {
        return {
            collection: this.requireOptions('collection'),
            id: this.requireOptions('id'),
        };
    }
    getRemovePermissionDuplicatesConfig() {
        return {
            keep: this.requireOptions('keep'),
        };
    }
    getWaitForServerReadyConfig() {
        return {
            interval: this.requireOptions('interval'),
            timeout: this.requireOptions('timeout'),
            successes: this.requireOptions('successes'),
        };
    }
    getConfigFileLoaderConfig() {
        return this.requireOptions('configPath');
    }
    getCollectionHooksConfig(collection) {
        var _a;
        const hooks = this.getOptions('hooks');
        if (!hooks) {
            return {};
        }
        // Type assertion is needed because the schema does not define the function arguments
        return ((_a = hooks[collection]) !== null && _a !== void 0 ? _a : {});
    }
    getSnapshotHooksConfig() {
        var _a;
        const hooks = this.getOptions('hooks');
        if (!hooks) {
            return {};
        }
        // Type assertion is needed because the schema does not define the function arguments
        return ((_a = hooks.snapshot) !== null && _a !== void 0 ? _a : {});
    }
    getCollectionsToProcess() {
        const collections = this.requireOptions('collections');
        if (!collections) {
            return [];
        }
        const exclude = this.requireOptions('excludeCollections');
        const only = this.requireOptions('onlyCollections');
        const list = only.length > 0 ? only : schema_1.CollectionsList;
        return list.filter((collection) => !exclude.includes(collection));
    }
    shouldSyncPolicyRoles() {
        return this.requireOptions('syncPolicyRoles');
    }
    shouldPreserveIds(collection) {
        const preserveIds = this.requireOptions('preserveIds');
        return (preserveIds === '*' ||
            preserveIds === 'all' ||
            preserveIds.includes(collection));
    }
    getOptions(name) {
        const options = this.flattenOptions();
        return options[name];
    }
    requireOptions(name) {
        const value = this.getOptions(name);
        if (value !== undefined) {
            return value;
        }
        throw new Error(`Missing option ${name}`);
    }
    /**
     * Options overridden in this order:
     * 1. Default options
     * 2. Config file options
     * 3. Program options
     * 4. Command options
     */
    flattenOptions() {
        var _a, _b, _c;
        let options = {};
        options = (0, deepmerge_1.default)(options, default_config_1.DefaultConfig);
        options = (0, deepmerge_1.default)(options, (_a = this.getFileOptions()) !== null && _a !== void 0 ? _a : {});
        options = (0, deepmerge_1.default)(options, (_b = this.programOptions) !== null && _b !== void 0 ? _b : {});
        options = (0, deepmerge_1.default)(options, (_c = this.commandOptions) !== null && _c !== void 0 ? _c : {});
        // Validate options
        return (0, helpers_1.zodParse)(options, schema_1.OptionsSchema, 'Options parsing');
    }
    getFileOptions() {
        var _a;
        const logger = this.getLogger();
        const customConfigPath = (_a = this.programOptions) === null || _a === void 0 ? void 0 : _a.configPath;
        const possiblePaths = [
            ...(customConfigPath ? [path_1.default.resolve(customConfigPath)] : []),
            ...default_config_1.DefaultConfigPaths.map((p) => path_1.default.resolve(p)),
        ];
        // Try to load the config file from the possible paths
        for (const configPath of possiblePaths) {
            const config = new config_file_loader_1.ConfigFileLoader(configPath).get();
            if (config) {
                logger.info(`Loaded config file from ${configPath}`);
                return config;
            }
        }
        logger.info(`No config file found. Tried path: \n- ${possiblePaths.join('\n- ')}`);
        return undefined;
    }
    /**
     * Returns a temporary logger as it may be changed by another one with specific options
     * See loader.ts file for more information
     */
    getLogger() {
        const logger = typedi_1.Container.get(logger_1.LoggerService);
        return logger.getChild('config');
    }
};
exports.ConfigService = ConfigService;
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], ConfigService.prototype, "getLoggerConfig", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], ConfigService.prototype, "shouldSortJson", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], ConfigService.prototype, "getCollectionsConfig", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], ConfigService.prototype, "getSnapshotConfig", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], ConfigService.prototype, "getSpecificationsConfig", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], ConfigService.prototype, "getSeedConfig", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], ConfigService.prototype, "getPushConfig", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", Object)
], ConfigService.prototype, "getDirectusConfig", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], ConfigService.prototype, "getUntrackConfig", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], ConfigService.prototype, "getRemovePermissionDuplicatesConfig", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], ConfigService.prototype, "getWaitForServerReadyConfig", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], ConfigService.prototype, "getConfigFileLoaderConfig", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Object)
], ConfigService.prototype, "getCollectionHooksConfig", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", Object)
], ConfigService.prototype, "getSnapshotHooksConfig", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], ConfigService.prototype, "getCollectionsToProcess", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], ConfigService.prototype, "shouldSyncPolicyRoles", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", void 0)
], ConfigService.prototype, "shouldPreserveIds", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], ConfigService.prototype, "flattenOptions", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", Object)
], ConfigService.prototype, "getFileOptions", null);
exports.ConfigService = ConfigService = __decorate([
    (0, typedi_1.Service)({ global: true })
], ConfigService);
//# sourceMappingURL=config.js.map