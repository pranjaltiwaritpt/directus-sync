"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DefaultConfig = exports.DefaultConfigPaths = void 0;
exports.DefaultConfigPaths = [
    './directus-sync.config.js',
    './directus-sync.config.cjs',
    './directus-sync.config.json',
];
const defaultDumpPath = './directus-config';
exports.DefaultConfig = {
    // Global
    debug: false,
    sortJson: false,
    // Pull, diff, push
    dumpPath: defaultDumpPath,
    maxPushRetries: 20,
    // Collections
    collectionsPath: 'collections',
    excludeCollections: [],
    onlyCollections: [],
    collections: true,
    syncPolicyRoles: true,
    preserveIds: [],
    // Snapshot
    snapshotPath: 'snapshot',
    snapshot: true,
    split: true,
    // Diff formatting
    prettyDiff: false,
    // Specifications
    specsPath: 'specs',
    specs: true,
    // Seed
    seedPath: `${defaultDumpPath}/seed`,
    // Diff, push
    force: false,
    // Remove Permission Duplicates
    keep: 'last',
    // Helpers: wait for server ready
    interval: 5,
    timeout: 90,
    successes: 1,
};
//# sourceMappingURL=default-config.js.map