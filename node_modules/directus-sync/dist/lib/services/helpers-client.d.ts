import { ExtensionClient } from './extension-client';
import { MigrationClient } from './migration-client';
import { ConfigService } from './config';
import { LoggerService, Logger } from './logger';
export declare class HelpersClient extends ExtensionClient {
    protected readonly config: ConfigService;
    protected readonly logger: Logger;
    constructor(migrationClient: MigrationClient, config: ConfigService, loggerService: LoggerService);
    removePermissionDuplicates(): Promise<void>;
    untrack(): Promise<void>;
    waitServerReady(): Promise<void>;
    protected checkServerStatus(): Promise<boolean>;
}
