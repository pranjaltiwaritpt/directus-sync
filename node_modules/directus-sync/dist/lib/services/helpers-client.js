"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.HelpersClient = void 0;
const extension_client_1 = require("./extension-client");
const migration_client_1 = require("./migration-client");
const typedi_1 = require("typedi");
const config_1 = require("./config");
const logger_1 = require("./logger");
const sdk_1 = require("@directus/sdk");
let HelpersClient = class HelpersClient extends extension_client_1.ExtensionClient {
    constructor(migrationClient, config, loggerService) {
        super(migrationClient);
        this.config = config;
        this.logger = loggerService.getChild('helpers-client');
    }
    removePermissionDuplicates() {
        return __awaiter(this, void 0, void 0, function* () {
            var _a, _b;
            const { keep } = this.config.getRemovePermissionDuplicatesConfig();
            const results = yield this.fetch('/helpers/permissions/duplicates', 'DELETE', { keep });
            for (const result of (_a = results.deletedPermissions) !== null && _a !== void 0 ? _a : []) {
                this.logger.info(`Deleted ${result.ids.length} duplicated permissions for policy ${(_b = result.policy) !== null && _b !== void 0 ? _b : null}, collection ${result.collection}, action ${result.action}`);
            }
        });
    }
    untrack() {
        return __awaiter(this, void 0, void 0, function* () {
            const { collection, id } = this.config.getUntrackConfig();
            yield this.fetch(`/table/${collection}/local_id/${id}`, 'DELETE');
            this.logger.info(`Untracked ${collection} ${id}`);
        });
    }
    waitServerReady() {
        return __awaiter(this, void 0, void 0, function* () {
            const { interval, timeout, successes } = this.config.getWaitForServerReadyConfig();
            const intervalMs = interval * 1000;
            const timeoutMs = timeout * 1000;
            const endAt = Date.now() + timeoutMs;
            let consecutiveSuccesses = 0;
            this.logger.info(`Waiting for server to be ready (timeout ${timeout}s, interval ${interval}s, successes ${successes})`);
            // Poll until ready or timeout
            while (Date.now() < endAt) {
                const ready = yield this.checkServerStatus().catch(() => false);
                consecutiveSuccesses = ready ? consecutiveSuccesses + 1 : 0;
                if (consecutiveSuccesses >= successes) {
                    this.logger.info('Server is ready');
                    return;
                }
                yield new Promise((resolve) => setTimeout(resolve, intervalMs));
            }
            throw new Error(`Timeout waiting for server to be ready after ${timeout}s`);
        });
    }
    checkServerStatus() {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                const client = yield this.migrationClient.get();
                const result = yield client.request((0, sdk_1.serverHealth)());
                const isOk = (result === null || result === void 0 ? void 0 : result.status) === 'ok';
                this.logger.debug(`Server status: ${result === null || result === void 0 ? void 0 : result.status}`);
                return isOk;
            }
            catch (e) {
                this.logger.debug(`Error checking server status: ${e}`);
                return false;
            }
        });
    }
};
exports.HelpersClient = HelpersClient;
exports.HelpersClient = HelpersClient = __decorate([
    (0, typedi_1.Service)({ global: true }),
    __metadata("design:paramtypes", [migration_client_1.MigrationClient,
        config_1.ConfigService,
        logger_1.LoggerService])
], HelpersClient);
//# sourceMappingURL=helpers-client.js.map