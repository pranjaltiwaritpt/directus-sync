"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.PoliciesDataClient = void 0;
const base_1 = require("../base");
const sdk_1 = require("@directus/sdk");
const typedi_1 = require("typedi");
const migration_client_1 = require("../../migration-client");
const logger_1 = require("../../logger");
const constants_1 = require("./constants");
const deepmerge_1 = __importDefault(require("deepmerge"));
const config_1 = require("../../config");
let PoliciesDataClient = class PoliciesDataClient extends base_1.DataClient {
    constructor(loggerService, migrationClient, config) {
        super(loggerService.getChild(constants_1.POLICIES_COLLECTION), migrationClient);
        this.config = config;
    }
    getDeleteCommand(itemId) {
        return (0, sdk_1.deletePolicy)(itemId);
    }
    getInsertCommand(item) {
        return (0, sdk_1.createPolicy)(item);
    }
    getQueryCommand(query) {
        // When role-policy attachments sync is disabled, omit the roles fields
        // entirely from the dump so they are neither tracked nor diffed.
        // See https://github.com/tractr/directus-sync/issues/199
        // Include roles.user so we can detect and skip user-attached accesses.
        const extraFields = this.config.shouldSyncPolicyRoles()
            ? ['*', 'roles.role', 'roles.user', 'roles.sort']
            : ['*'];
        return (0, sdk_1.readPolicies)((0, deepmerge_1.default)(query, {
            fields: extraFields,
        }));
    }
    getUpdateCommand(itemId, diffItem) {
        return __awaiter(this, void 0, void 0, function* () {
            // When role-policy attachments sync is disabled, drop the roles diff
            // entirely so existing attachments on the target are left untouched.
            // See https://github.com/tractr/directus-sync/issues/199
            if (!this.config.shouldSyncPolicyRoles() && diffItem.roles) {
                const _a = diffItem, { roles: _ignored } = _a, rest = __rest(_a, ["roles"]);
                diffItem = rest;
            }
            // Explicit update of the roles field (many-to-many relation)
            // Issue : https://github.com/tractr/directus-sync/issues/148
            if (diffItem.roles) {
                // retrieve the access ids
                const rolesDiff = yield this.getRolesDiff(itemId, diffItem.roles);
                if (!this.isRolesDiffEmpty(rolesDiff)) {
                    diffItem = Object.assign(Object.assign({}, diffItem), { roles: rolesDiff });
                }
            }
            return (0, sdk_1.updatePolicy)(itemId, diffItem);
        });
    }
    isRolesDiffEmpty(diff) {
        return (diff.create.length === 0 &&
            diff.update.length === 0 &&
            diff.delete.length === 0);
    }
    getRolesDiff(itemId, accesses) {
        return __awaiter(this, void 0, void 0, function* () {
            const rolesAccessesIdsMap = yield this.getRolesAccessesIdsMap(itemId);
            const create = [];
            const update = [];
            const remove = [];
            for (const access of accesses) {
                const role = access.role;
                if (!role) {
                    // Role is null for public policy only. This should not be updated nor removed.
                    continue;
                }
                const accessId = rolesAccessesIdsMap.get(role);
                if (accessId) {
                    update.push(Object.assign(Object.assign({}, access), { id: accessId }));
                }
                else {
                    create.push(access);
                }
            }
            for (const [role, accessId] of rolesAccessesIdsMap.entries()) {
                if (!accesses.some((access) => access.role === role)) {
                    remove.push(accessId);
                }
            }
            return { create, update, delete: remove };
        });
    }
    getRolesAccessesIdsMap(policyId) {
        return __awaiter(this, void 0, void 0, function* () {
            const client = yield this.migrationClient.get();
            // This should not return twice the same role
            const policy = yield client.request((0, sdk_1.readPolicy)(policyId, {
                fields: ['roles.id', 'roles.role'],
            }));
            return policy.roles.reduce((acc, access) => {
                if (access.role) {
                    acc.set(access.role, access.id);
                }
                return acc;
            }, new Map());
        });
    }
};
exports.PoliciesDataClient = PoliciesDataClient;
exports.PoliciesDataClient = PoliciesDataClient = __decorate([
    (0, typedi_1.Service)(),
    __metadata("design:paramtypes", [logger_1.LoggerService,
        migration_client_1.MigrationClient,
        config_1.ConfigService])
], PoliciesDataClient);
//# sourceMappingURL=data-client.js.map