"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.PoliciesIdMapperClient = void 0;
const base_1 = require("../base");
const typedi_1 = require("typedi");
const constants_1 = require("./constants");
const migration_client_1 = require("../../migration-client");
const logger_1 = require("../../logger");
const roles_1 = require("../roles");
const sdk_1 = require("@directus/sdk");
let PoliciesIdMapperClient = class PoliciesIdMapperClient extends base_1.IdMapperClient {
    constructor(migrationClient, loggerService, rolesIdMapperClient) {
        super(migrationClient, loggerService.getChild(constants_1.POLICIES_COLLECTION), constants_1.POLICIES_COLLECTION);
        this.rolesIdMapperClient = rolesIdMapperClient;
        this.adminPolicyFetched = false;
        this.adminPolicyPlaceholder = '_sync_default_admin_policy';
        this.publicPolicyFetched = false;
        this.publicPolicyPlaceholder = '_sync_default_public_policy';
    }
    /**
     * Force admin and public policies placeholders for the admin and public policies.
     */
    create(localId, syncId) {
        const _super = Object.create(null, {
            create: { get: () => super.create }
        });
        return __awaiter(this, void 0, void 0, function* () {
            const adminPolicyId = yield this.getAdminPolicyId();
            if (localId === adminPolicyId) {
                return yield _super.create.call(this, localId, this.adminPolicyPlaceholder);
            }
            const publicPolicyId = yield this.getPublicPolicyId();
            if (localId === publicPolicyId) {
                return yield _super.create.call(this, localId, this.publicPolicyPlaceholder);
            }
            return yield _super.create.call(this, localId, syncId);
        });
    }
    /**
     * Create the sync id of the admin and public policies on the fly, as it already has been synced.
     */
    getBySyncId(syncId) {
        const _super = Object.create(null, {
            getBySyncId: { get: () => super.getBySyncId },
            create: { get: () => super.create }
        });
        return __awaiter(this, void 0, void 0, function* () {
            const idMap = yield _super.getBySyncId.call(this, syncId);
            if (!idMap) {
                // Automatically create the default admin policy id map if it doesn't exist
                if (syncId === this.adminPolicyPlaceholder) {
                    const adminPolicyId = yield this.getAdminPolicyId();
                    if (adminPolicyId) {
                        yield _super.create.call(this, adminPolicyId, this.adminPolicyPlaceholder);
                        this.logger.debug(`Created admin policy id map with local id ${adminPolicyId}`);
                        return yield _super.getBySyncId.call(this, syncId);
                    }
                }
                // Automatically create the default public policy id map if it doesn't exist
                else if (syncId === this.publicPolicyPlaceholder) {
                    const publicPolicyId = yield this.getPublicPolicyId();
                    if (publicPolicyId) {
                        yield _super.create.call(this, publicPolicyId, this.publicPolicyPlaceholder);
                        this.logger.debug(`Created public policy id map with local id ${publicPolicyId}`);
                        return yield _super.getBySyncId.call(this, syncId);
                    }
                }
            }
            return idMap;
        });
    }
    /**
     * Returns the default admin policy, attached to the admin role.
     */
    getAdminPolicyId() {
        return __awaiter(this, void 0, void 0, function* () {
            if (!this.adminPolicyFetched) {
                const directus = yield this.migrationClient.get();
                const adminRoleId = yield this.rolesIdMapperClient.getAdminRoleId();
                const [policy] = yield directus.request((0, sdk_1.readPolicies)({
                    fields: ['id'],
                    filter: {
                        _and: [
                            { roles: { role: { _eq: adminRoleId } } },
                            { admin_access: { _eq: true } },
                        ],
                    },
                    limit: 1,
                }));
                if (!policy) {
                    this.logger.debug('Cannot find the admin policy attached to the admin role');
                }
                this.adminPolicyId = policy === null || policy === void 0 ? void 0 : policy.id;
                this.adminPolicyFetched = true;
            }
            return this.adminPolicyId;
        });
    }
    /**
     * Returns the default public policy, where role = null
     */
    getPublicPolicyId() {
        return __awaiter(this, void 0, void 0, function* () {
            if (!this.publicPolicyFetched) {
                const directus = yield this.migrationClient.get();
                const [policy] = yield directus.request((0, sdk_1.readPolicies)({
                    fields: ['id'],
                    filter: {
                        _and: [
                            { roles: { role: { _null: true } } },
                            // Exclude user-attached accesses (role=null, user=uuid) so they
                            // are not mistaken for the public policy access (role=null, user=null).
                            { roles: { user: { _null: true } } },
                            {
                                _or: [
                                    { roles: { sort: { _eq: 1 } } },
                                    // Allow to find the policy even if the sort is not set.
                                    // This may occur after migrations from 10.x to 11.x
                                    { roles: { sort: { _null: true } } },
                                ],
                            },
                        ],
                    },
                    limit: 1,
                }));
                if (!policy) {
                    this.logger.debug('Cannot find the public policy');
                }
                this.publicPolicyId = policy === null || policy === void 0 ? void 0 : policy.id;
                this.publicPolicyFetched = true;
            }
            return this.publicPolicyId;
        });
    }
};
exports.PoliciesIdMapperClient = PoliciesIdMapperClient;
exports.PoliciesIdMapperClient = PoliciesIdMapperClient = __decorate([
    (0, typedi_1.Service)(),
    __metadata("design:paramtypes", [migration_client_1.MigrationClient,
        logger_1.LoggerService,
        roles_1.RolesIdMapperClient])
], PoliciesIdMapperClient);
//# sourceMappingURL=id-mapper-client.js.map