import { Command, DataClient, Query, WithoutIdAndSyncId } from '../base';
import { MigrationClient } from '../../migration-client';
import { DirectusTranslation } from './interfaces';
import { LoggerService } from '../../logger';
export declare class TranslationsDataClient extends DataClient<DirectusTranslation> {
    constructor(loggerService: LoggerService, migrationClient: MigrationClient);
    protected getDeleteCommand(itemId: string): import("@directus/sdk").RestCommand<void, unknown>;
    protected getInsertCommand(item: WithoutIdAndSyncId<DirectusTranslation>): import("@directus/sdk").RestCommand<{
        value: string;
        key: string;
        id: string;
        language: string;
    }, unknown>;
    protected getQueryCommand(query: Query<DirectusTranslation>): import("@directus/sdk").RestCommand<{
        [x: string]: never;
    }[], unknown>;
    protected getUpdateCommand(itemId: string, diffItem: Partial<WithoutIdAndSyncId<DirectusTranslation>>): import("@directus/sdk").RestCommand<{
        value: string;
        key: string;
        id: string;
        language: string;
    }, unknown> | [Command<{
        id: string;
        language: string;
        key: string;
        value: string;
    }>, Command<{
        id: string;
        language: string;
        key: string;
        value: string;
    }>];
}
