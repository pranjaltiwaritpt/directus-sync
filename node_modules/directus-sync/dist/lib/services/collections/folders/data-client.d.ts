import { DataClient, Query, WithoutIdAndSyncId } from '../base';
import { MigrationClient } from '../../migration-client';
import { DirectusFolder } from './interfaces';
import { LoggerService } from '../../logger';
export declare class FoldersDataClient extends DataClient<DirectusFolder> {
    constructor(loggerService: LoggerService, migrationClient: MigrationClient);
    protected getDeleteCommand(itemId: string): import("@directus/sdk").RestCommand<void, unknown>;
    protected getInsertCommand(item: WithoutIdAndSyncId<DirectusFolder>): import("@directus/sdk").RestCommand<{
        id: string;
        name: string;
        parent: string | {
            id: string;
            name: string;
            parent: string | /*elided*/ any | null;
        } | null;
    }, unknown>;
    protected getQueryCommand(query: Query<DirectusFolder>): import("@directus/sdk").RestCommand<{
        [x: string]: never;
    }[], unknown>;
    protected getUpdateCommand(itemId: string, diffItem: Partial<WithoutIdAndSyncId<DirectusFolder>>): import("@directus/sdk").RestCommand<{
        id: string;
        name: string;
        parent: string | {
            id: string;
            name: string;
            parent: string | /*elided*/ any | null;
        } | null;
    }, unknown>;
}
