import { DataLoader, WithSyncIdAndWithoutId } from '../base';
import { DirectusOperation } from './interfaces';
import { ConfigService } from '../../config';
import { MigrationClient } from '../../migration-client';
export declare class OperationsDataLoader extends DataLoader<DirectusOperation> {
    constructor(config: ConfigService, migrationClient: MigrationClient);
    protected getSortFunction(): (a: WithSyncIdAndWithoutId<DirectusOperation>, b: WithSyncIdAndWithoutId<DirectusOperation>) => number;
}
