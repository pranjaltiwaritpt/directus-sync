import { DataDiffer, DirectusId } from '../base';
import { PermissionsDataLoader } from './data-loader';
import { PermissionsDataClient } from './data-client';
import { PermissionsIdMapperClient } from './id-mapper-client';
import { PermissionsDataMapper } from './data-mapper';
import { DirectusPermission } from './interfaces';
import { LoggerService } from '../../logger';
export declare class PermissionsDataDiffer extends DataDiffer<DirectusPermission> {
    constructor(loggerService: LoggerService, dataLoader: PermissionsDataLoader, dataClient: PermissionsDataClient, dataMapper: PermissionsDataMapper, idMapper: PermissionsIdMapperClient);
    /**
     * Add more fields in the request to get id field
     * https://github.com/directus/directus/issues/21965
     */
    protected getExistingIds(localIds: string[]): Promise<{
        id: DirectusId;
    }[]>;
}
