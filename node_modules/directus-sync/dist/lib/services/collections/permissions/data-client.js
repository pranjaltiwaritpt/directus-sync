"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.PermissionsDataClient = void 0;
const base_1 = require("../base");
const sdk_1 = require("@directus/sdk");
const typedi_1 = require("typedi");
const migration_client_1 = require("../../migration-client");
const logger_1 = require("../../logger");
const constants_1 = require("./constants");
let PermissionsDataClient = class PermissionsDataClient extends base_1.DataClient {
    constructor(loggerService, migrationClient) {
        super(loggerService.getChild(constants_1.PERMISSIONS_COLLECTION), migrationClient);
    }
    /**
     * Returns the admins permissions. These are static permissions that are not stored in the database.
     * We must discard them as they can't be updated and have no id.
     */
    query(query) {
        const _super = Object.create(null, {
            query: { get: () => super.query }
        });
        return __awaiter(this, void 0, void 0, function* () {
            const values = yield _super.query.call(this, query);
            return values.filter((value) => !!value.id);
        });
    }
    getDeleteCommand(itemId) {
        return (0, sdk_1.deletePermission)(itemId);
    }
    getInsertCommand(item) {
        return __awaiter(this, void 0, void 0, function* () {
            // Check if a similar permission already exists and remove it
            // Discussed in https://github.com/directus/directus/issues/21965
            const directus = yield this.migrationClient.get();
            const permissions = yield directus.request((0, sdk_1.readPermissions)({
                filter: {
                    policy: { _eq: item.policy },
                    collection: { _eq: item.collection },
                    action: { _eq: item.action },
                },
                fields: ['id'],
            }));
            const existingPermissions = permissions.map((p) => p.id).filter(Boolean);
            if (existingPermissions.length) {
                this.logger.warn(`Found duplicate permissions for ${item.collection}.${item.action} with policy ${item.policy}. Deleting them.`);
                return [
                    (0, sdk_1.deletePermissions)(existingPermissions),
                    (0, sdk_1.createPermission)(item),
                ];
            }
            return (0, sdk_1.createPermission)(item);
        });
    }
    getQueryCommand(query) {
        return (0, sdk_1.readPermissions)(Object.assign({}, query));
    }
    getUpdateCommand(itemId, diffItem) {
        return (0, sdk_1.updatePermission)(itemId, diffItem);
    }
};
exports.PermissionsDataClient = PermissionsDataClient;
exports.PermissionsDataClient = PermissionsDataClient = __decorate([
    (0, typedi_1.Service)(),
    __metadata("design:paramtypes", [logger_1.LoggerService, migration_client_1.MigrationClient])
], PermissionsDataClient);
//# sourceMappingURL=data-client.js.map