import { DataLoader, WithSyncIdAndWithoutId } from '../base';
import { DirectusPermission } from './interfaces';
import { ConfigService } from '../../config';
import { MigrationClient } from '../../migration-client';
export declare class PermissionsDataLoader extends DataLoader<DirectusPermission> {
    constructor(config: ConfigService, migrationClient: MigrationClient);
    protected getSortFunction(): (a: WithSyncIdAndWithoutId<DirectusPermission>, b: WithSyncIdAndWithoutId<DirectusPermission>) => number;
}
