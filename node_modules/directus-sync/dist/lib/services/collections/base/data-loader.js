"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.DataLoader = void 0;
const fs_extra_1 = require("fs-extra");
const helpers_1 = require("../../../helpers");
class DataLoader {
    constructor(filePath, migrationClient, hooks, config) {
        this.filePath = filePath;
        this.migrationClient = migrationClient;
        this.hooks = hooks;
        this.config = config;
    }
    /**
     * Returns the source data from the dump file, using readFileSync
     * and passes it through the data transformer.
     */
    getSourceData() {
        return __awaiter(this, void 0, void 0, function* () {
            var _a;
            const { onLoad } = this.hooks;
            const loadedData = (_a = (0, fs_extra_1.readJsonSync)(this.filePath, { throws: false })) !== null && _a !== void 0 ? _a : [];
            return onLoad
                ? yield onLoad(loadedData, yield this.migrationClient.get())
                : loadedData;
        });
    }
    /**
     * Save the data to the dump file. The data is passed through the data transformer.
     */
    saveData(data) {
        return __awaiter(this, void 0, void 0, function* () {
            // Sort data by _syncId to avoid git changes
            data.sort(this.getSortFunction());
            const { onSave } = this.hooks;
            const transformedData = onSave
                ? yield onSave(data, yield this.migrationClient.get())
                : data;
            (0, helpers_1.writeJsonSync)(this.filePath, transformedData, this.config.shouldSortJson());
        });
    }
    /**
     * Returns a function to sort the data before saving it.
     */
    getSortFunction() {
        return (a, b) => a._syncId.localeCompare(b._syncId);
    }
}
exports.DataLoader = DataLoader;
//# sourceMappingURL=data-loader.js.map