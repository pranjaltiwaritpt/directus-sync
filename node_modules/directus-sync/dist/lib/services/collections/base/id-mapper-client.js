"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.IdMapperClient = void 0;
const extension_client_1 = require("../../extension-client");
class IdMapperClient extends extension_client_1.ExtensionClient {
    constructor(migrationClient, logger, table) {
        super(migrationClient);
        this.logger = logger;
        this.table = table;
        /**
         * Cache for id maps
         */
        this.cache = {
            bySyncId: new Map(),
            byLocalId: new Map(),
        };
    }
    getBySyncId(syncId) {
        return __awaiter(this, void 0, void 0, function* () {
            // Try to get from cache
            const cached = this.cache.bySyncId.get(syncId);
            if (cached) {
                return cached;
            }
            // Try to get from server
            const idMap = yield this.fetch(`/table/${this.table}/sync_id/${syncId}`).catch((error) => {
                if (error.message === extension_client_1.NO_ID_MAP_MESSAGE) {
                    return undefined;
                }
                throw error;
            });
            // Add to cache
            if (idMap) {
                this.addToCache(idMap);
            }
            return idMap;
        });
    }
    getByLocalId(localId) {
        return __awaiter(this, void 0, void 0, function* () {
            // Try to get from cache
            const cached = this.cache.byLocalId.get(localId);
            if (cached) {
                return cached;
            }
            // Try to get from server
            const idMap = yield this.fetch(`/table/${this.table}/local_id/${localId}`).catch((error) => {
                if (error.message === extension_client_1.NO_ID_MAP_MESSAGE) {
                    return undefined;
                }
                throw error;
            });
            // Add to cache
            if (idMap) {
                this.addToCache(idMap);
            }
            return idMap;
        });
    }
    getAll() {
        return __awaiter(this, void 0, void 0, function* () {
            const all = yield this.fetch(`/table/${this.table}`);
            // Add to cache
            all.forEach((idMap) => {
                this.addToCache(idMap);
            });
            return all;
        });
    }
    create(localId, syncId) {
        return __awaiter(this, void 0, void 0, function* () {
            const data = yield this.fetch(`/table/${this.table}`, 'POST', {
                table: this.table, // TODO: Could remove this (to be tested)
                local_id: localId,
                sync_id: syncId,
            });
            return data.sync_id;
        });
    }
    removeBySyncId(syncId) {
        return __awaiter(this, void 0, void 0, function* () {
            yield this.fetch(`/table/${this.table}/sync_id/${syncId}`, 'DELETE');
            this.removeCacheBySyncId(syncId);
        });
    }
    removeByLocalId(localId) {
        return __awaiter(this, void 0, void 0, function* () {
            yield this.fetch(`/table/${this.table}/local_id/${localId}`, 'DELETE');
            this.removeCacheByLocalId(localId);
        });
    }
    // Cache
    clearCache() {
        this.cache.byLocalId.clear();
        this.cache.bySyncId.clear();
    }
    addToCache(idMap) {
        this.cache.bySyncId.set(idMap.sync_id, idMap);
        this.cache.byLocalId.set(idMap.local_id, idMap);
    }
    removeFromCache(idMap) {
        this.cache.bySyncId.delete(idMap.sync_id);
        this.cache.byLocalId.delete(idMap.local_id);
    }
    removeCacheByLocalId(localId) {
        const idMap = this.cache.byLocalId.get(localId);
        if (idMap) {
            this.removeFromCache(idMap);
        }
    }
    removeCacheBySyncId(syncId) {
        const idMap = this.cache.bySyncId.get(syncId);
        if (idMap) {
            this.removeFromCache(idMap);
        }
    }
}
exports.IdMapperClient = IdMapperClient;
//# sourceMappingURL=id-mapper-client.js.map