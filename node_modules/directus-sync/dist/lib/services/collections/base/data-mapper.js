"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.DataMapper = void 0;
const id_mapper_client_1 = require("./id-mapper-client");
const helpers_1 = require("./helpers");
class DataMapper {
    constructor(logger) {
        this.logger = logger;
        /**
         * These field will be ignored when saving and restoring the data.
         */
        this.fieldsToIgnore = [];
        /**
         * Returns a map of fields and id mapper to use when mapping the ids of the items.
         */
        this.idMappers = {};
    }
    /**
     * Returns the items with the ids mapped to the sync ids,
     * without the fields to ignore.
     */
    mapIdsToSyncIdAndRemoveIgnoredFields(items) {
        return __awaiter(this, void 0, void 0, function* () {
            const output = [];
            for (const item of items) {
                const withoutFields = this.removeFieldsToIgnore(item);
                const newItem = yield this.mapLocalIdToSyncIdIfPossible(withoutFields);
                output.push(newItem);
            }
            return output;
        });
    }
    /**
     * Discard the fields to ignore from the items.
     */
    removeFieldsToIgnore(item) {
        const newItem = Object.assign({}, item);
        for (const field of this.fieldsToIgnore) {
            delete newItem[field];
        }
        return newItem;
    }
    /**
     * Map the sync id of the item to the local id.
     * Returns undefined if the item has no sync id.
     * This allows to create items by order of dependencies.
     */
    mapSyncIdToLocalId(item) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield (0, helpers_1.applyMappers)(item, this.getSyncIdToLocalIdMapper());
        });
    }
    /**
     * Build once the mapper functions from sync to local id.
     */
    getSyncIdToLocalIdMapper() {
        if (!this.syncIdToLocalIdMappers) {
            const callback = (idMapper, field) => (id) => __awaiter(this, void 0, void 0, function* () {
                if (this.isDynamicId(id)) {
                    this.logger.debug(`Value '${id}' for field '${field}' is dynamic, skipping mapping.`);
                    return id;
                }
                const idMap = yield idMapper.getBySyncId(id.toString());
                if (!idMap) {
                    this.logger.warn(`No id map found for ${field}:${id}, will try on the next round`);
                    return undefined;
                }
                return idMap.local_id;
            });
            const predicate = (value) => typeof value === 'object' && !(value instanceof id_mapper_client_1.IdMapperClient);
            this.syncIdToLocalIdMappers = (0, helpers_1.bindMappers)(this.idMappers, callback, predicate);
        }
        return this.syncIdToLocalIdMappers;
    }
    /**
     * Returns true if the id is dynamic, i.e. it is a template.
     */
    isDynamicId(id) {
        return id.toString().trim().startsWith('{{');
    }
    /**
     * Map the ids of the item to the sync ids.
     * For new items, the original id is used.
     */
    mapLocalIdToSyncIdIfPossible(item) {
        return __awaiter(this, void 0, void 0, function* () {
            const output = yield (0, helpers_1.applyMappers)(item, this.getLocalIdToSyncIdMapper());
            if (!output) {
                throw new Error('A mapper returned undefined. This should not happen.');
            }
            return output;
        });
    }
    /**
     * Build once the mapper functions from local to sync id.
     */
    getLocalIdToSyncIdMapper() {
        if (!this.localIdToSyncIdMappers) {
            const callback = (idMapper, field) => (id) => __awaiter(this, void 0, void 0, function* () {
                const idMap = yield idMapper.getByLocalId(id.toString());
                if (!idMap) {
                    this.logger.debug(`No id map found for ${field}:${id}`);
                    return id;
                }
                return idMap.sync_id;
            });
            const predicate = (value) => typeof value === 'object' && !(value instanceof id_mapper_client_1.IdMapperClient);
            this.localIdToSyncIdMappers = (0, helpers_1.bindMappers)(this.idMappers, callback, predicate);
        }
        return this.localIdToSyncIdMappers;
    }
}
exports.DataMapper = DataMapper;
//# sourceMappingURL=data-mapper.js.map