import * as DirectusSdk from '@directus/sdk';
import { AuthenticationClient, DirectusClient, RestClient } from '@directus/sdk';
import { LoggerService, Logger } from './logger';
import { ConfigService } from './config';
import { DirectusSchema } from './interfaces';
export declare class MigrationClient {
    protected readonly config: ConfigService;
    protected readonly logger: Logger;
    protected adminRoleId: string | undefined;
    protected client: (DirectusClient<DirectusSchema> & RestClient<DirectusSchema> & AuthenticationClient<DirectusSchema>) | undefined;
    constructor(config: ConfigService, loggerService: LoggerService);
    get(): Promise<DirectusSdk.DirectusClient<any> & DirectusSdk.RestClient<any> & DirectusSdk.AuthenticationClient<any>>;
    /**
     * This method clears the cache of the Directus instance
     */
    clearCache(): Promise<void>;
    /**
     * Best-effort clearing of the Directus system cache.
     * Uses the SDK helper when available; otherwise logs a debug note.
     */
    protected tryClearSystemCache(): Promise<void>;
    protected createClient(): Promise<DirectusSdk.DirectusClient<any> & DirectusSdk.RestClient<any> & DirectusSdk.AuthenticationClient<any>>;
    /**
     * Get the server version
     */
    protected getDirectusVersion(): Promise<string>;
    /**
     * This method compares the Directus instance version with the given one.
     */
    protected compareWithDirectusVersion(version: string): Promise<'equal' | 'greater' | 'smaller'>;
    /**
     * This method validate that the Directus instance version is compatible with the current CLI version.
     */
    validateDirectusVersion(): Promise<void>;
}
