import { MigrationClient } from './migration-client';
export declare const NO_ID_MAP_MESSAGE = "No id map found";
/**
 * This class provides an interface to interact with the Directus extension sync endpoints.
 */
export declare abstract class ExtensionClient {
    protected readonly migrationClient: MigrationClient;
    protected readonly extensionUri = "/directus-extension-sync";
    constructor(migrationClient: MigrationClient);
    protected fetch<T = unknown>(uri: string, method?: RequestInit['method'], payload?: unknown, options?: RequestInit): Promise<T>;
    protected getUrlAndToken(): Promise<{
        url: string;
        token: string;
    }>;
    protected getFetch(): Promise<typeof fetch>;
}
