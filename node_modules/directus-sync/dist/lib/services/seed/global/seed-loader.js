"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SeedLoader = void 0;
const typedi_1 = require("typedi");
const helpers_1 = require("../../../helpers");
const config_1 = require("../../config");
const interfaces_1 = require("../interfaces");
const Fs = __importStar(require("fs-extra"));
const typescript_cacheable_1 = require("typescript-cacheable");
const directus_structure_1 = require("./directus-structure");
const logger_1 = require("../../logger");
const path_1 = __importDefault(require("path"));
let SeedLoader = class SeedLoader {
    constructor(loggerService, config) {
        this.loggerService = loggerService;
        this.config = config;
        this.logger = this.loggerService.getChild('seed-loader');
    }
    loadFromFiles() {
        const { paths } = this.config.getSeedConfig();
        const seeds = [];
        for (const path of paths) {
            // Test if the path exists
            if (!Fs.pathExistsSync(path)) {
                this.logger.warn(`Seed path does not exist: ${path}`);
            }
            const rawSeeds = (0, helpers_1.loadJsonFilesRecursivelyWithSchema)(path, interfaces_1.SeedsFileSchema, 'Load seeds').flat(2);
            // Apply absolute file path to data
            const folderPath = this.getFolderPath(path);
            const seedsWithAbsoluteFilePath = rawSeeds.map((seed) => this.applyFilePathToData(seed, folderPath));
            seeds.push(...seedsWithAbsoluteFilePath);
        }
        // Order seeds by meta.insert_order
        // Leave seeds with undefined insert_order at the end, in the order they were loaded
        // For seeds with insert_order, sort them by insert_order in ascending order
        const unsortableSeeds = seeds.filter((seed) => { var _a; return ((_a = seed.meta) === null || _a === void 0 ? void 0 : _a.insert_order) === undefined; });
        const sortableSeeds = seeds.filter((seed) => { var _a; return ((_a = seed.meta) === null || _a === void 0 ? void 0 : _a.insert_order) !== undefined; });
        sortableSeeds.sort((a, b) => {
            return a.meta.insert_order - b.meta.insert_order;
        });
        const sortedSeeds = [...sortableSeeds, ...unsortableSeeds];
        // Merge seeds with the same collection name
        const mergedSeeds = sortedSeeds.reduce((acc, seed) => {
            const existing = acc.find((s) => s.collection === seed.collection);
            if (existing) {
                this.mergeSeeds(existing, seed);
            }
            else {
                acc.push(seed);
            }
            return acc;
        }, []);
        // Apply defaults for directus collections
        mergedSeeds.forEach((seed) => this.applyDirectusCollectionDefaults(seed));
        return mergedSeeds;
    }
    /**
     * Merge two seeds
     *  - merge data with concat
     *  - merge meta.create, meta.update, meta.delete with AND logic
     *  - merge meta.preserve_ids with OR logic
     */
    mergeSeeds(existing, seed) {
        existing.data = [...existing.data, ...seed.data];
        existing.meta.create = existing.meta.create && seed.meta.create;
        existing.meta.update = existing.meta.update && seed.meta.update;
        existing.meta.delete = existing.meta.delete && seed.meta.delete;
        existing.meta.preserve_ids =
            existing.meta.preserve_ids || seed.meta.preserve_ids;
        existing.meta.ignore_on_update = [
            ...existing.meta.ignore_on_update,
            ...seed.meta.ignore_on_update,
        ];
    }
    /**
     * Applies the defaults for directus collections.
     *  - ignore_on_update: add the fields that are ignored on update in the Directus structure
     */
    applyDirectusCollectionDefaults(seed) {
        if (this.isDirectusCollection(seed.collection)) {
            const structure = directus_structure_1.DirectusNativeStructure[seed.collection];
            seed.meta.ignore_on_update = [
                ...structure.ignoreOnUpdate,
                ...seed.meta.ignore_on_update,
            ];
        }
    }
    /**
     * Denotes if the collection is a directus collection.
     */
    isDirectusCollection(collection) {
        return directus_structure_1.SupportedDirectusCollections.includes(collection);
    }
    /**
     * Returns the directory path if a file path is provided
     * Otherwise, returns the path as is
     * Test with Fs
     */
    getFolderPath(filePath) {
        return Fs.pathExistsSync(filePath) && Fs.statSync(filePath).isFile()
            ? path_1.default.dirname(filePath)
            : filePath;
    }
    /**
     * If the seed has a file path _file_path, replace it with the absolute path
     */
    applyFilePathToData(seed, folderPath) {
        const data = seed.data.map((item) => {
            if (item._file_path) {
                const isAbsolute = path_1.default.isAbsolute(item._file_path);
                item._file_path = isAbsolute
                    ? item._file_path
                    : path_1.default.join(folderPath, item._file_path);
            }
            return item;
        });
        return Object.assign(Object.assign({}, seed), { data });
    }
};
exports.SeedLoader = SeedLoader;
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", Array)
], SeedLoader.prototype, "loadFromFiles", null);
exports.SeedLoader = SeedLoader = __decorate([
    (0, typedi_1.Service)({ global: true }),
    __metadata("design:paramtypes", [logger_1.LoggerService,
        config_1.ConfigService])
], SeedLoader);
//# sourceMappingURL=seed-loader.js.map