"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SchemaClient = exports.Type = void 0;
const typedi_1 = require("typedi");
const constants_1 = require("../constants");
const typescript_cacheable_1 = require("typescript-cacheable");
const snapshot_1 = require("../../snapshot");
Object.defineProperty(exports, "Type", { enumerable: true, get: function () { return snapshot_1.Type; } });
const directus_structure_1 = require("./directus-structure");
const logger_1 = require("../../logger");
// TypeDI doest not detect SchemaClient identifier automatically. Need to set it manually using id option.
let SchemaClient = class SchemaClient {
    constructor(loggerService, snapshotClient) {
        this.loggerService = loggerService;
        this.snapshotClient = snapshotClient;
        this.logger = this.loggerService.getChild('schema-client');
    }
    /**
     * Returns the list of fields of type "many-to-one" of a model.
     */
    getRelationFields(model) {
        return __awaiter(this, void 0, void 0, function* () {
            if (this.isDirectusCollection(model)) {
                return this.getDirectusCollectionRelationFields(model);
            }
            const snapshot = yield this.snapshotClient.getSnapshot();
            return snapshot.relations
                .filter((r) => r.collection === model)
                .map((r) => r.field);
        });
    }
    /**
     * Returns the target model of a many-to-one relation.
     * Throw an error if the relation is not many-to-one
     * or the model does not exist or the field does not exist.
     */
    getTargetModel(model, field) {
        return __awaiter(this, void 0, void 0, function* () {
            if (this.isDirectusCollection(model)) {
                const relation = this.getDirectusCollectionTargetModel(model, field);
                if (!relation) {
                    throw new Error(`Relation ${model}.${field} does not exist in the Directus structure`);
                }
                return relation;
            }
            const snapshot = yield this.snapshotClient.getSnapshot();
            const relation = snapshot.relations.find((r) => r.collection === model && r.field === field);
            if (!relation) {
                throw new Error(`Relation ${model}.${field} does not exist in the snapshot`);
            }
            return relation.related_collection;
        });
    }
    /**
     * Returns the primary field of a model.
     */
    getPrimaryField(model) {
        return __awaiter(this, void 0, void 0, function* () {
            if (this.isDirectusCollection(model)) {
                return this.getDirectusCollectionPrimaryField(model);
            }
            const snapshot = yield this.snapshotClient.getSnapshot();
            const fields = snapshot.fields.filter((c) => c.collection === model);
            const primaryField = fields.find((f) => { var _a; return (_a = f.schema) === null || _a === void 0 ? void 0 : _a.is_primary_key; });
            if (!primaryField) {
                throw new Error(`Primary field not found in ${model}`);
            }
            return { name: primaryField.field, type: primaryField.type };
        });
    }
    /**
     * Denotes if the collection is a directus collection.
     */
    isDirectusCollection(collection) {
        if (collection.startsWith(constants_1.DIRECTUS_COLLECTIONS_PREFIX)) {
            if (!directus_structure_1.SupportedDirectusCollections.includes(collection)) {
                throw new Error(`Unsupported Directus collection by seed command: ${collection}. Supported collections are: ${directus_structure_1.SupportedDirectusCollections.join(', ')}`);
            }
            return true;
        }
        return false;
    }
    /**
     * Returns the list of fields of type "many-to-one" of a directus collection.
     */
    getDirectusCollectionRelationFields(model) {
        const structure = directus_structure_1.DirectusNativeStructure[model];
        return structure.relations.map((r) => r.field);
    }
    /**
     * Returns the target model of a directus collection.
     */
    getDirectusCollectionTargetModel(model, field) {
        var _a;
        const structure = directus_structure_1.DirectusNativeStructure[model];
        return (_a = structure.relations.find((r) => r.field === field)) === null || _a === void 0 ? void 0 : _a.collection;
    }
    /**
     * Returns the primary field type of a directus model.
     */
    getDirectusCollectionPrimaryField(model) {
        const structure = directus_structure_1.DirectusNativeStructure[model];
        return structure.primaryField;
    }
};
exports.SchemaClient = SchemaClient;
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], SchemaClient.prototype, "getRelationFields", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, String]),
    __metadata("design:returntype", Promise)
], SchemaClient.prototype, "getTargetModel", null);
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", Promise)
], SchemaClient.prototype, "getPrimaryField", null);
exports.SchemaClient = SchemaClient = __decorate([
    (0, typedi_1.Service)({ global: true, id: constants_1.SCHEMA_CLIENT }),
    __metadata("design:paramtypes", [logger_1.LoggerService,
        snapshot_1.SnapshotClient])
], SchemaClient);
//# sourceMappingURL=schema-client.js.map