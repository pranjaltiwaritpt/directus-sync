import { ContainerInstance } from 'typedi';
import { LoggerService, Logger } from '../../logger';
import { Seed } from '../interfaces';
import { SeedLoader } from './seed-loader';
export declare class SeedClient {
    protected readonly loggerService: LoggerService;
    protected readonly seedLoader: SeedLoader;
    protected readonly logger: Logger;
    constructor(loggerService: LoggerService, seedLoader: SeedLoader);
    /**
     * Denotes if seeds exist
     */
    hasSeeds(): boolean;
    /**
     * Display diff for all seeds
     */
    diff(): Promise<void>;
    /**
     * Display diff for a seed
     */
    protected diffSeed(seed: Seed): Promise<void>;
    /**
     * Push all seeds
     */
    push(): Promise<boolean>;
    protected pushSeed(seed: Seed): Promise<boolean>;
    cleanUp(): Promise<void>;
    protected cleanUpCollection(seed: Seed): Promise<void>;
    protected createContainer(seed: Seed): Promise<ContainerInstance>;
}
