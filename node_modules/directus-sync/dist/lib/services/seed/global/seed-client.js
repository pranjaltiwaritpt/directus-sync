"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SeedClient = void 0;
const typedi_1 = __importStar(require("typedi"));
const constants_1 = require("../constants");
const logger_1 = require("../../logger");
const seed_loader_1 = require("./seed-loader");
const collections_1 = require("../collections");
let SeedClient = class SeedClient {
    constructor(loggerService, seedLoader) {
        this.loggerService = loggerService;
        this.seedLoader = seedLoader;
        this.logger = this.loggerService.getChild('seed-client');
    }
    /**
     * Denotes if seeds exist
     */
    hasSeeds() {
        const seeds = this.seedLoader.loadFromFiles();
        return seeds.length > 0;
    }
    /**
     * Display diff for all seeds
     */
    diff() {
        return __awaiter(this, void 0, void 0, function* () {
            const seeds = this.seedLoader.loadFromFiles();
            if (!seeds.length) {
                this.logger.warn('No seeds found');
            }
            for (const seed of seeds) {
                yield this.diffSeed(seed);
            }
        });
    }
    /**
     * Display diff for a seed
     */
    diffSeed(seed) {
        return __awaiter(this, void 0, void 0, function* () {
            const container = yield this.createContainer(seed);
            const seedCollection = container.get(collections_1.SeedCollection);
            yield seedCollection.diff(seed.data);
            container.reset();
        });
    }
    /**
     * Push all seeds
     */
    push() {
        return __awaiter(this, void 0, void 0, function* () {
            const seeds = this.seedLoader.loadFromFiles();
            if (!seeds.length) {
                this.logger.warn('No seeds found');
                return false;
            }
            let retry = false;
            for (const seed of seeds) {
                retry = (yield this.pushSeed(seed)) || retry;
            }
            return retry;
        });
    }
    pushSeed(seed) {
        return __awaiter(this, void 0, void 0, function* () {
            const container = yield this.createContainer(seed);
            const seedCollection = container.get(collections_1.SeedCollection);
            const retry = yield seedCollection.push(seed.data);
            container.reset();
            return retry;
        });
    }
    cleanUp() {
        return __awaiter(this, void 0, void 0, function* () {
            const seeds = this.seedLoader.loadFromFiles();
            if (!seeds.length) {
                return;
            }
            for (const seed of seeds) {
                yield this.cleanUpCollection(seed);
            }
        });
    }
    cleanUpCollection(seed) {
        return __awaiter(this, void 0, void 0, function* () {
            const container = yield this.createContainer(seed);
            const seedCollection = container.get(collections_1.SeedCollection);
            yield seedCollection.cleanUp();
            container.reset();
        });
    }
    createContainer(seed) {
        return __awaiter(this, void 0, void 0, function* () {
            // Get the collection and meta
            const { collection, meta } = seed;
            // Create a new container for this seed
            const container = typedi_1.default.of(collection);
            container.set(constants_1.COLLECTION, collection);
            container.set(constants_1.META, meta);
            // Logger is global service; nothing to set here anymore.
            // Initialize the data mapper
            const dataMapper = container.get(collections_1.SeedDataMapper);
            yield dataMapper.initialize();
            return container;
        });
    }
};
exports.SeedClient = SeedClient;
exports.SeedClient = SeedClient = __decorate([
    (0, typedi_1.Service)({ global: true }),
    __metadata("design:paramtypes", [logger_1.LoggerService,
        seed_loader_1.SeedLoader])
], SeedClient);
//# sourceMappingURL=seed-client.js.map