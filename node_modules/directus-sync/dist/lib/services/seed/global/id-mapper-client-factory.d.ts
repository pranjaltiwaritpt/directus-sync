import { IdMapperClient } from '../../collections';
import { MigrationClient } from '../../migration-client';
import { LoggerService } from '../../logger';
export declare class SeedIdMapperClient extends IdMapperClient {
}
/**
 * Factory for SeedIdMapperClient instances
 * We use a global service to ensure that the same instance is used for the same collection
 */
export declare class SeedIdMapperClientFactory {
    private readonly migrationClient;
    private readonly loggerService;
    constructor(migrationClient: MigrationClient, loggerService: LoggerService);
    /**
     * Get or create a SeedIdMapperClient instance for a specific collection
     */
    forCollection(collection: string): SeedIdMapperClient;
}
