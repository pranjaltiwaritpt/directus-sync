"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.fileItemToFormData = fileItemToFormData;
exports.getFileAsBlob = getFileAsBlob;
const Fs = __importStar(require("fs-extra"));
const mime_types_1 = require("mime-types");
/**
 * Convert a FileItem to a FormData object
 * @param object - The FileItem to convert
 * @returns The FormData object
 */
function fileItemToFormData(object) {
    var _a;
    const formData = new FormData();
    const file = getFileAsBlob(object);
    // Add properties first (https://github.com/directus/directus/discussions/10130#discussioncomment-2216554)
    for (const [key, value] of Object.entries(object)) {
        if (key === '_file_path') {
            continue;
        }
        if (typeof value === 'string') {
            formData.append(key, value);
        }
        else if (typeof value === 'object' && value !== null) {
            formData.append(key, JSON.stringify(value));
        }
        else {
            formData.append(key, (_a = value === null || value === void 0 ? void 0 : value.toString()) !== null && _a !== void 0 ? _a : '');
        }
    }
    // Add file last
    formData.append('file', file);
    return formData;
}
/**
 * Read a file from the file system and return it as a Blob
 * Check if _file_path is defined and exists
 * @returns The Blob
 */
function getFileAsBlob(object) {
    var _a, _b;
    if (!object._file_path) {
        throw new Error('File path (key _file_path) is not defined');
    }
    if (!Fs.pathExistsSync(object._file_path)) {
        throw new Error(`File path (key _file_path) does not exist: ${object._file_path}`);
    }
    const file = Fs.readFileSync(object._file_path);
    const type = (_b = (_a = object.type) !== null && _a !== void 0 ? _a : ((0, mime_types_1.lookup)(object._file_path) || undefined)) !== null && _b !== void 0 ? _b : 'application/octet-stream';
    return new Blob([file], { type });
}
//# sourceMappingURL=files.js.map