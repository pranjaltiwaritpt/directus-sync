"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SnapshotClient = void 0;
const typedi_1 = require("typedi");
const migration_client_1 = require("../migration-client");
const sdk_1 = require("@directus/sdk");
const path_1 = __importDefault(require("path"));
const fs_extra_1 = require("fs-extra");
const helpers_1 = require("../../helpers");
const config_1 = require("../config");
const typescript_cacheable_1 = require("typescript-cacheable");
const logger_1 = require("../logger");
const pretty_diff_1 = require("./pretty-diff");
const SNAPSHOT_JSON = 'snapshot.json';
const INFO_JSON = 'info.json';
const COLLECTIONS_DIR = 'collections';
const FIELDS_DIR = 'fields';
const RELATIONS_DIR = 'relations';
let SnapshotClient = class SnapshotClient {
    constructor(config, loggerService, migrationClient) {
        this.migrationClient = migrationClient;
        this.ignoredCollection = 'directus_sync_id_map';
        this.logger = loggerService.getChild('snapshot');
        const { dumpPath, splitFiles, force, prettyDiff } = config.getSnapshotConfig();
        this.dumpPath = dumpPath;
        this.splitFiles = splitFiles;
        this.force = force;
        this.prettyDiff = prettyDiff;
        this.hooks = config.getSnapshotHooksConfig();
        this.sortJson = config.shouldSortJson();
    }
    /**
     * Save the snapshot locally
     */
    pull() {
        return __awaiter(this, void 0, void 0, function* () {
            const snapshot = yield this.getSnapshot();
            const { onSave } = this.hooks;
            const transformedSnapshot = onSave
                ? yield onSave(snapshot, yield this.migrationClient.get())
                : snapshot;
            const numberOfFiles = this.saveData(transformedSnapshot);
            this.logger.debug(`Saved ${numberOfFiles} file${numberOfFiles > 1 ? 's' : ''} to ${this.dumpPath}`);
        });
    }
    /**
     * Apply the snapshot from the dump files.
     */
    push() {
        return __awaiter(this, void 0, void 0, function* () {
            const diff = yield this.diffSnapshot();
            if (!this.hasDiff(diff)) {
                this.logger.debug('No changes to apply');
            }
            else {
                const directus = yield this.migrationClient.get();
                yield directus.request((0, sdk_1.schemaApply)(diff));
                this.logger.info('Changes applied');
            }
        });
    }
    /**
     * Diff the snapshot from the dump file.
     */
    diff() {
        return __awaiter(this, void 0, void 0, function* () {
            const diff = yield this.diffSnapshot();
            if (!this.hasDiff(diff)) {
                this.logger.debug('No changes to apply');
            }
            else {
                const { collections, fields, relations } = diff.diff;
                if (collections) {
                    this.logger.info(`Found ${collections.length} change${collections.length > 1 ? 's' : ''} in collections`);
                }
                else {
                    this.logger.info('No changes in collections');
                }
                if (fields) {
                    this.logger.info(`Found ${fields.length} change${fields.length > 1 ? 's' : ''} in fields`);
                }
                else {
                    this.logger.info('No changes in fields');
                }
                if (relations) {
                    this.logger.info(`Found ${relations.length} change${relations.length > 1 ? 's' : ''} in relations`);
                }
                else {
                    this.logger.info('No changes in relations');
                }
                if (this.prettyDiff) {
                    this.logger.info((0, pretty_diff_1.getDiffMessage)(diff.diff));
                }
                else {
                    this.logger.debug(diff.diff, 'Diff');
                }
            }
        });
    }
    /**
     * Get the snapshot from the Directus instance.
     */
    getSnapshot() {
        return __awaiter(this, void 0, void 0, function* () {
            const directus = yield this.migrationClient.get();
            const snapshot = yield directus.request((0, sdk_1.schemaSnapshot)()); // Get better types
            return this.removeIgnoredCollection(snapshot);
        });
    }
    /**
     * Remove some items from the snapshot or diff, directus_sync collection for example
     */
    removeIgnoredCollection(input) {
        var _a, _b, _c, _d, _e, _f;
        const name = this.ignoredCollection;
        input.collections =
            (_b = (_a = input.collections) === null || _a === void 0 ? void 0 : _a.filter((c) => c.collection !== name)) !== null && _b !== void 0 ? _b : [];
        input.fields = (_d = (_c = input.fields) === null || _c === void 0 ? void 0 : _c.filter((f) => f.collection !== name)) !== null && _d !== void 0 ? _d : [];
        input.relations =
            (_f = (_e = input.relations) === null || _e === void 0 ? void 0 : _e.filter((r) => r.collection !== name)) !== null && _f !== void 0 ? _f : [];
        return input;
    }
    /**
     * Save the data to the dump file. The data is passed through the data transformer.
     * Returns the number of saved items.
     */
    saveData(data) {
        // Clean directory
        (0, fs_extra_1.removeSync)(this.dumpPath);
        (0, fs_extra_1.mkdirpSync)(this.dumpPath);
        // Save data
        if (this.splitFiles) {
            const files = this.decomposeData(data);
            for (const file of files) {
                const filePath = path_1.default.join(this.dumpPath, file.path);
                const dirPath = path_1.default.dirname(filePath);
                (0, fs_extra_1.mkdirpSync)(dirPath);
                (0, helpers_1.writeJsonSync)(filePath, file.content, this.sortJson);
            }
            return files.length;
        }
        else {
            const filePath = path_1.default.join(this.dumpPath, SNAPSHOT_JSON);
            (0, helpers_1.writeJsonSync)(filePath, data, this.sortJson);
            return 1;
        }
    }
    /**
     * Decompose the snapshot into a collection of files.
     */
    decomposeData(data) {
        const { collections, fields, relations } = data, info = __rest(data, ["collections", "fields", "relations"]);
        const files = [
            { path: INFO_JSON, content: info },
        ];
        /*
         * Split collections
         * Folder and collections may have the same name (with different casing).
         * Also, some file systems are case-insensitive.
         */
        const existingCollections = new Set();
        for (const collection of collections) {
            const suffix = this.getSuffix(collection.collection, existingCollections);
            files.push({
                path: `${COLLECTIONS_DIR}/${collection.collection}${suffix}.json`,
                content: collection,
            });
        }
        /*
         * Split fields
         * Groups and fields may have the same name (with different casing).
         * Also, some file systems are case-insensitive.
         * Therefore, inside a collection we have to deal with names conflicts.
         */
        const existingFiles = new Set();
        for (const field of fields) {
            const suffix = this.getSuffix(`${field.collection}/${field.field}`, existingFiles);
            files.push({
                path: `${FIELDS_DIR}/${field.collection}/${field.field}${suffix}.json`,
                content: field,
            });
        }
        /*
         * Split relations
         * There should not be any conflicts here, but we still split them for consistency.
         */
        const existingRelations = new Set();
        for (const relation of relations) {
            const suffix = this.getSuffix(`${relation.collection}/${relation.field}`, existingRelations);
            files.push({
                path: `${RELATIONS_DIR}/${relation.collection}/${relation.field}${suffix}.json`,
                content: relation,
            });
        }
        return files;
    }
    /**
     * Get the suffix that should be added to the field name in order to avoid conflicts.
     */
    getSuffix(baseName, existing) {
        const base = baseName.toLowerCase(); // Some file systems are case-insensitive
        let suffix = '';
        if (existing.has(base)) {
            let i = 2;
            while (existing.has(`${base}_${i}`)) {
                i++;
            }
            suffix = `_${i}`;
        }
        existing.add(`${base}${suffix}`);
        return suffix;
    }
    /**
     * Get the diff from Directus instance
     * From Directus 11.4.1, the diff is not returned if there are no changes.
     */
    diffSnapshot() {
        return __awaiter(this, void 0, void 0, function* () {
            const directus = yield this.migrationClient.get();
            const { onLoad } = this.hooks;
            const snapshot = this.loadData();
            const transformedSnapshot = onLoad
                ? yield onLoad(snapshot, yield this.migrationClient.get())
                : snapshot;
            const diff = (yield directus.request((0, sdk_1.schemaDiff)(transformedSnapshot, this.force)));
            // Filter ignored collection
            if (diff === null || diff === void 0 ? void 0 : diff.diff) {
                diff.diff = this.removeIgnoredCollection(diff.diff);
            }
            return diff;
        });
    }
    hasDiff(diff) {
        if (!(diff === null || diff === void 0 ? void 0 : diff.diff)) {
            return false;
        }
        if (Object.keys(diff.diff).length === 0) {
            return false;
        }
        if (diff.diff.collections.length === 0 &&
            diff.diff.fields.length === 0 &&
            diff.diff.relations.length === 0) {
            return false;
        }
        return true;
    }
    /**
     * Load the snapshot from the dump file or the decomposed files.
     */
    loadData() {
        if (this.splitFiles) {
            const collections = (0, helpers_1.loadJsonFilesRecursively)(path_1.default.join(this.dumpPath, COLLECTIONS_DIR));
            const fields = (0, helpers_1.loadJsonFilesRecursively)(path_1.default.join(this.dumpPath, FIELDS_DIR));
            const relations = (0, helpers_1.loadJsonFilesRecursively)(path_1.default.join(this.dumpPath, RELATIONS_DIR));
            const info = (0, fs_extra_1.readJsonSync)(path_1.default.join(this.dumpPath, INFO_JSON));
            return Object.assign(Object.assign({}, info), { collections, fields, relations });
        }
        else {
            const filePath = path_1.default.join(this.dumpPath, SNAPSHOT_JSON);
            return (0, fs_extra_1.readJsonSync)(filePath, 'utf-8');
        }
    }
};
exports.SnapshotClient = SnapshotClient;
__decorate([
    (0, typescript_cacheable_1.Cacheable)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", Promise)
], SnapshotClient.prototype, "getSnapshot", null);
exports.SnapshotClient = SnapshotClient = __decorate([
    (0, typedi_1.Service)({ global: true }),
    __metadata("design:paramtypes", [config_1.ConfigService,
        logger_1.LoggerService,
        migration_client_1.MigrationClient])
], SnapshotClient);
//# sourceMappingURL=snapshot-client.js.map