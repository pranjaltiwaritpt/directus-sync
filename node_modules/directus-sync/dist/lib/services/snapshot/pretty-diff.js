"use strict";
/* eslint-disable @typescript-eslint/no-explicit-any */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getDiffMessage = getDiffMessage;
const chalk_1 = __importDefault(require("chalk"));
const DiffKind = {
    /** indicates a newly added property/element */
    NEW: 'N',
    /** indicates a property/element was deleted */
    DELETE: 'D',
    /** indicates a property/element was edited */
    EDIT: 'E',
    /** indicates a change occurred within an array */
    ARRAY: 'A',
};
// -------------------------------------------------------------------------------------------------
/**
 * Pretty print the diff using the same format as Directus CLI:
 * https://github.com/directus/directus/blob/main/api/src/cli/commands/schema/apply.ts#L84-L168
 */
function getDiffMessage(snapshotDiff) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m;
    const sections = [];
    if (snapshotDiff.collections.length > 0) {
        const lines = [chalk_1.default.underline.bold('Collections:')];
        for (const { collection, diff } of snapshotDiff.collections) {
            if (((_a = diff[0]) === null || _a === void 0 ? void 0 : _a.kind) === DiffKind.EDIT) {
                lines.push(`  - ${chalk_1.default.magenta('Update')} ${collection}`);
                for (const change of diff) {
                    if (change.kind === DiffKind.EDIT) {
                        const path = formatPath(change.path);
                        lines.push(`    - Set ${path} to ${change.rhs}`);
                    }
                }
            }
            else if (((_b = diff[0]) === null || _b === void 0 ? void 0 : _b.kind) === DiffKind.DELETE) {
                lines.push(`  - ${chalk_1.default.red('Delete')} ${collection}`);
            }
            else if (((_c = diff[0]) === null || _c === void 0 ? void 0 : _c.kind) === DiffKind.NEW) {
                lines.push(`  - ${chalk_1.default.green('Create')} ${collection}`);
            }
            else if (((_d = diff[0]) === null || _d === void 0 ? void 0 : _d.kind) === DiffKind.ARRAY) {
                lines.push(`  - ${chalk_1.default.magenta('Update')} ${collection}`);
            }
        }
        sections.push(lines.join('\n'));
    }
    if (snapshotDiff.fields.length > 0) {
        const lines = [chalk_1.default.underline.bold('Fields:')];
        for (const { collection, field, diff } of snapshotDiff.fields) {
            if (((_e = diff[0]) === null || _e === void 0 ? void 0 : _e.kind) === DiffKind.EDIT || isNestedMetaUpdate(diff[0])) {
                lines.push(`  - ${chalk_1.default.magenta('Update')} ${collection}.${field}`);
                for (const change of diff) {
                    const path = formatPath(change.path);
                    if (change.kind === DiffKind.EDIT) {
                        lines.push(`    - Set ${path} to ${change.rhs}`);
                    }
                    else if (change.kind === DiffKind.DELETE) {
                        lines.push(`    - Remove ${path}`);
                    }
                    else if (change.kind === DiffKind.NEW) {
                        lines.push(`    - Add ${path} and set it to ${change.rhs}`);
                    }
                }
            }
            else if (((_f = diff[0]) === null || _f === void 0 ? void 0 : _f.kind) === DiffKind.DELETE) {
                lines.push(`  - ${chalk_1.default.red('Delete')} ${collection}.${field}`);
            }
            else if (((_g = diff[0]) === null || _g === void 0 ? void 0 : _g.kind) === DiffKind.NEW) {
                lines.push(`  - ${chalk_1.default.green('Create')} ${collection}.${field}`);
            }
            else if (((_h = diff[0]) === null || _h === void 0 ? void 0 : _h.kind) === DiffKind.ARRAY) {
                lines.push(`  - ${chalk_1.default.magenta('Update')} ${collection}.${field}`);
            }
        }
        sections.push(lines.join('\n'));
    }
    if (snapshotDiff.relations.length > 0) {
        const lines = [chalk_1.default.underline.bold('Relations:')];
        for (const { collection, field, related_collection, diff, } of snapshotDiff.relations) {
            const relatedCollection = formatRelatedCollection(related_collection);
            if (((_j = diff[0]) === null || _j === void 0 ? void 0 : _j.kind) === DiffKind.EDIT) {
                lines.push(`  - ${chalk_1.default.magenta('Update')} ${collection}.${field}${relatedCollection}`);
                for (const change of diff) {
                    if (change.kind === DiffKind.EDIT) {
                        const path = formatPath(change.path);
                        lines.push(`    - Set ${path} to ${change.rhs}`);
                    }
                }
            }
            else if (((_k = diff[0]) === null || _k === void 0 ? void 0 : _k.kind) === DiffKind.DELETE) {
                lines.push(`  - ${chalk_1.default.red('Delete')} ${collection}.${field}${relatedCollection}`);
            }
            else if (((_l = diff[0]) === null || _l === void 0 ? void 0 : _l.kind) === DiffKind.NEW) {
                lines.push(`  - ${chalk_1.default.green('Create')} ${collection}.${field}${relatedCollection}`);
            }
            else if (((_m = diff[0]) === null || _m === void 0 ? void 0 : _m.kind) === DiffKind.ARRAY) {
                lines.push(`  - ${chalk_1.default.magenta('Update')} ${collection}.${field}${relatedCollection}`);
            }
        }
        sections.push(lines.join('\n'));
    }
    const message = 'The following changes will be applied:\n\n' + sections.join('\n\n') + '\n';
    return message;
}
function formatPath(path) {
    if (path.length === 1) {
        return path.toString();
    }
    return path.slice(1).join('.');
}
function formatRelatedCollection(relatedCollection) {
    // Related collection doesn't exist for a2o relationship types
    if (relatedCollection) {
        return ` → ${relatedCollection}`;
    }
    return '';
}
function isNestedMetaUpdate(diff) {
    if (!diff)
        return false;
    if (diff.kind !== DiffKind.NEW && diff.kind !== DiffKind.DELETE)
        return false;
    if (!diff.path || diff.path.length < 2 || diff.path[0] !== 'meta')
        return false;
    return true;
}
//# sourceMappingURL=pretty-diff.js.map