import { z, ZodSchema } from 'zod';
import { LoggerConfigTransport, Logger } from './services';
export declare function createDumpFolders(): void;
/**
 * Log message to debug or info, depending on the debug flag
 */
export declare function debugOrInfoLogger(logger: Logger): (info: boolean, message: string) => void;
/**
 * Load all JSON files from a directory recursively.
 */
export declare function loadJsonFilesRecursively<T>(dirPath: string): T[];
/**
 * Recursively load json files from a directory and validate them against a zod schema
 * The root path could be a folder or a JSON file
 */
export declare function loadJsonFilesRecursivelyWithSchema<T extends ZodSchema>(rootPath: string, schema: T, errorContext?: string): z.infer<T>[];
/**
 * Validate an object against a zod schema and format the error if it fails
 */
export declare function zodParse<T extends ZodSchema>(payload: unknown, schema: T, errorContext?: string): z.infer<T>;
export declare function getPinoTransport(): LoggerConfigTransport;
/**
 * Recursively sort object keys to ensure stable JSON output.
 * Arrays are processed element-wise. Primitives are returned as-is.
 */
export declare function sortObjectDeep<T>(value: T): T;
/**
 * Write JSON to disk with stable key ordering.
 */
export declare function writeJsonSync(filePath: string, data: unknown, sort: boolean): void;
