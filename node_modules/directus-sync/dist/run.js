"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.run = run;
const lib_1 = require("./lib");
const typedi_1 = require("typedi");
function getLogger() {
    return typedi_1.Container.has(lib_1.LOGGER_TRANSPORT)
        ? typedi_1.Container.get(lib_1.LoggerService)
        : console;
}
function run() {
    return (0, lib_1.createProgram)()
        .parseAsync(process.argv)
        .catch((error) => {
        const logger = getLogger();
        logger.error(error);
        process.exit(1);
    })
        .then(() => {
        const logger = getLogger();
        logger.info(`✅  Done!`);
        process.exit(0);
    });
}
//# sourceMappingURL=run.js.map