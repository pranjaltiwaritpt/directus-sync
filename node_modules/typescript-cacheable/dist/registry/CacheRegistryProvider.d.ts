import { Scope } from '../Scope';
import { CacheRegistry } from './CacheRegistry';
export declare class CacheRegistryProvider {
    private static global;
    private static local;
    static forScope(scope: Scope): CacheRegistry;
}
